name: CI/CD Pipeline

on:
  push:
    branches:
      - main

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: my-app   # must be lowercase

jobs:
  build-and-scan:
    name: Build, Scan & Push Docker Image
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_PAT }}

      - name: Build & Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ghcr.io/manasab805/my-app:main
            ghcr.io/manasab805/my-app:sha-${{ github.sha }}
            - name: Build & Push Docker Image


      - name: Set up Trivy
        uses: aquasecurity/trivy-action@master

      - name: Run Trivy Scan
        run: |
          trivy image --exit-code 1 --severity HIGH,CRITICAL ${{ env.REGISTRY }}/${{ github.repository }}/my-app:sha-${{ github.sha }}

  update-deployment:
    name: Update Kubernetes Deployment
    runs-on: ubuntu-latest
    needs: build-and-scan

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set Git config
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"

      - name: Sync with remote main
        run: |
          git fetch origin main
          git reset --hard origin/main

      - name: Update Deployment Image
        run: |
          NEW_IMAGE="${{ env.REGISTRY }}/${{ github.repository }}/my-app:sha-${{ github.sha }}"
          echo "Updating deployment to use image: $NEW_IMAGE"
          sed -i "s|image: .*|image: ${NEW_IMAGE}|g" kubernetes/deployment.yaml
          grep -A 1 "image:" kubernetes/deployment.yaml

      - name: Commit and Push Deployment Update
        run: |
          git add kubernetes/deployment.yaml
          git commit -m "Update deployment image to $NEW_IMAGE" || echo "No changes to commit"
          git push origin main
